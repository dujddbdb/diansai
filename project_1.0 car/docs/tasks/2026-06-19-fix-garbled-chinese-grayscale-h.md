# 2026-06-19 修复 grayscale.h 中文乱码

## 涉及文件
- `bsp/grayscale.h` — 全文重写（仅修复注释，不改代码）

## 修改前状态
文件 `bsp/grayscale.h` 中的中文注释全部显示为乱码。经分析，文件存在混合编码问题：
- 部分中文字符（如"感为"）为正确的 UTF-8 编码
- 大部分中文字符为原始 GBK 字节，被嵌入 UTF-8 文件中，导致显示为 Unicode 扩展字符（如"ͨ"、"Ҷ"等）或 U+FFFD 替换字符

## 修改内容
根据部分可读字符、上下文语义、传感器 API 文档惯例，逐行还原所有中文注释为正确的 UTF-8 中文文本。主要修正内容：
- 文件头注释：传感器名称、硬件引脚说明、工作原理
- 宏定义注释：ADC 分辨率、灰度阈值、卡尔曼滤波标定参数说明
- 结构体注释：字段含义（原始值、归一化值、校准值、阈值等）
- 函数注释：初始化、校准、任务、数据获取接口的详细说明

## 验证
- 所有宏定义值、函数签名、结构体字段完全保留，16 项关键代码字符串校验全部通过
- 文件为合法 UTF-8 编码，Read 工具显示中文正常，无替换字符

## 注意事项
- 未修改任何代码、宏值或函数声明
- 还原的中文文本基于上下文推测，如有与原意不符之处需对照官方文档修正
